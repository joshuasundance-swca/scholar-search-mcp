name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  COVERAGE_FAIL_UNDER: '87'
  GH_AW_VERSION: v0.71.1

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - python-version: "3.10"
            coverage-gate: false
            heavy-checks: false
            verify-lockfile: false
            docker-candidate: false
          - python-version: "3.11"
            coverage-gate: false
            heavy-checks: false
            verify-lockfile: false
            docker-candidate: false
          - python-version: "3.12"
            coverage-gate: false
            heavy-checks: false
            verify-lockfile: false
            docker-candidate: false
          - python-version: "3.13"
            coverage-gate: false
            heavy-checks: false
            verify-lockfile: false
            docker-candidate: false
          - python-version: "3.14"
            coverage-gate: true
            heavy-checks: true
            verify-lockfile: true
            docker-candidate: true

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install gh-aw
        if: matrix.verify-lockfile
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh extension install github/gh-aw --pin "$GH_AW_VERSION"

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all]"

      - name: Verify installed dependencies
        run: python -m pip check

      - name: Verify agentic workflow lockfile is up to date
        if: matrix.verify-lockfile
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh aw compile test-paper-chaser --dir .github/workflows
          pre-commit run --files .github/workflows/test-paper-chaser.lock.yml || true
          pre-commit run --files .github/workflows/test-paper-chaser.lock.yml
          git diff --exit-code -- .github/workflows/test-paper-chaser.lock.yml

      - name: Run pre-commit
        run: pre-commit run --all-files

      - name: Run tests
        if: ${{ !matrix.coverage-gate }}
        run: python -m pytest

      - name: Run tests with coverage gate
        if: matrix.coverage-gate
        run: |
          python -m pytest \
            --cov=paper_chaser_mcp \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=${{ env.COVERAGE_FAIL_UNDER }}

      - name: Validate deployment assets
        if: matrix.heavy-checks
        run: python scripts/validate_deployment.py --skip-docker

      - name: Validate default Docker runtime
        if: matrix.docker-candidate
        run: |
          python scripts/validate_deployment.py \
            --skip-az \
            --require-docker \
            --image-tag paper-chaser-mcp:validation-314-default

      - name: Build package
        if: matrix.heavy-checks
        run: python -m build

      - name: Audit dependencies
        if: matrix.heavy-checks
        run: |
          # Temporary ignore: pip-audit flags CVE-2026-4539 for transitive
          # Pygments releases, but no fixed upstream version is published yet.
          python -m pip_audit . --progress-spinner off --ignore-vuln CVE-2026-4539

      - name: Run Bandit
        run: python -m bandit -c pyproject.toml -r paper_chaser_mcp
